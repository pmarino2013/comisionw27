/* const calculo = require('./funciones');

let base, altura;

base = parseFloat(prompt("Ingrese la base del triángulo: "));
altura = parseFloat(prompt("Ingrese la altura del triángulo: "));

console.log(`El área del triángulo de base ${base} y altura ${altura} es: ${calculo.calcularAreaTriangulo(base, altura)}`); */

